<UserControl x:Class="Bug_Tracker.Controls.HomePageNavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bug_Tracker.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="AliceBlue" HorizontalAlignment="left">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bug Tracker" HorizontalAlignment="Left" FontSize="20" Margin="40, 20, 40, 40"/>

        <StackPanel Grid.Row="1">

            <RadioButton Content="Account" Style="{StaticResource NavButton}"/>
            <RadioButton Content="Home" Style="{StaticResource NavButton}"/>
            <RadioButton Content="Projects" Style="{StaticResource NavButton}"/>
            <RadioButton Content="Tickets" Style="{StaticResource NavButton}"/>

        </StackPanel>
    </Grid>
</UserControl>
