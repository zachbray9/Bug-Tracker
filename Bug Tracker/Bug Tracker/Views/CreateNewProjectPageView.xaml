<UserControl x:Class="Bug_Tracker.Views.CreateNewProjectPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bug_Tracker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel>
            <Label Content="Project Name"/>
            <TextBox Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <Label Content="Description"/>
            <TextBox Text="{Binding ProjectDescription, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
            <Button Content="Create Project" Style="{StaticResource GenericButton}" Command="{Binding CreateNewProjectCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
