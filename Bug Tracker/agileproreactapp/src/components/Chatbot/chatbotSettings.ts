import { Settings } from "react-chatbotify";

export const ChatbotSettings: Settings = {
    header: {
        showAvatar: true,
        title: "AgileBot",
    },
    tooltip: {
        mode: "NEVER",
        text: "Hello"
    },
    chatHistory: {
        disabled: true
    }
}