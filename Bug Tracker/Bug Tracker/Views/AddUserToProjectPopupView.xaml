<UserControl x:Class="Bug_Tracker.Views.AddUserToProjectPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bug_Tracker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="40">
            <TextBlock Text="Add Someone To Project" FontSize="16" FontFamily="Arial" FontWeight="SemiBold"/>

            <TextBlock Text="Name or email" FontFamily="Arial" Margin="0, 20"/>
            <StackPanel>
                <TextBox Style="{StaticResource FocusChangeableTextBox}" Text="{Binding SearchQuery}"/>

                <ItemsControl ItemsSource="{Binding SearchResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding FullName}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
